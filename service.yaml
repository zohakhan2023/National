apiVersion: v1
kind: Service
metadata:
  name: national-tsa
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: national-tsa
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
  externalTrafficPolicy: Cluster
